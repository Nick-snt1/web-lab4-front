import { SignUp } from "./Register";
import { Navbar } from "../navbar/Navbar";

export const RegisterPage = () => {
    return (
        <div>
            <Navbar />
            <SignUp />
        </div>
    );
}